/// Represents the available WhatsApp Cloud API environments.
enum Environment {
  /// Production environment for real application use
  production,

  /// Sandbox environment for development and testing
  sandbox,
}